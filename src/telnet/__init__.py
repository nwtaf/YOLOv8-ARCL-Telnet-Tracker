# __init__.py
from .UI import emit, receive_data, startup_function, TextHandler, tn, undesirable_strings, host, port, password

# in src/tracking.py, the following line is used to import the telnet module:
# from .telnet_client import *